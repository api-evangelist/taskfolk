generated: '2026-08-20'
method: generated
description: >-
  Packaged Agent Skills for Taskfolk. One is the provider's own, saved verbatim; three are
  generated by the API Evangelist enrichment pipeline and grounded in operations that exist
  verbatim in openapi/taskfolk-product-api-openapi.yml. The contract declares NO operationIds on
  any of its 187 operations, so every skill binds by METHOD + path — real, greppable, and the
  only stable handle the contract offers.
skills:
  - file: taskfolk-product.skill.md
    name: taskfolk-product
    method: searched
    source: https://taskfolk.ai/api/skill/taskfolk-product.skill.md
    verbatim: true
    api: openapi/taskfolk-product-api-openapi.yml
    description: >-
      The provider's OWN published skill, saved verbatim. Sign up, create a workspace, mint a
      tfk_live_ key, and connect Claude Code / Cursor / MCP. The MCP server card points at this
      URL as its skillBundle. Re-fetching the same URL WITH a key and a workspace slug returns a
      workspace-customised guide — a genuinely unusual design: the skill is a live endpoint, not
      a static file.
    operations: ['GET /v1/me']
  - file: taskfolk-agent-session-lifecycle.md
    name: taskfolk-agent-session-lifecycle
    method: generated
    api: openapi/taskfolk-product-api-openapi.yml
    description: Claim an issue as an agent, heartbeat, report cost, comment, transition, finish honestly.
    operations:
      - 'GET /v1/me'
      - 'GET /v1/workspaces/{slug}/agent-sessions'
      - 'POST /v1/workspaces/{slug}/agent-sessions'
      - 'PATCH /v1/workspaces/{slug}/agent-sessions/{id}'
      - 'POST /v1/workspaces/{slug}/agent-sessions/{id}/usage'
      - 'POST /v1/workspaces/{slug}/projects/{key}/issues/{issueKey}/comments'
      - 'POST /v1/workspaces/{slug}/projects/{key}/issues/{issueKey}/transition'
  - file: taskfolk-triage-and-plan.md
    name: taskfolk-triage-and-plan
    method: generated
    api: openapi/taskfolk-product-api-openapi.yml
    description: Search before creating, create, label, route, sprint, and read the report that says whether it lands.
    operations:
      - 'GET /v1/workspaces/{slug}/search'
      - 'POST /v1/workspaces/{slug}/projects/{key}/issues'
      - 'POST /v1/workspaces/{slug}/projects/{key}/issues/bulk'
      - 'PATCH /v1/workspaces/{slug}/projects/{key}/issues/{issueKey}'
      - 'POST /v1/workspaces/{slug}/projects/{key}/sprints'
      - 'GET /v1/workspaces/{slug}/projects/{key}/reports/{report}'
  - file: taskfolk-safe-writes-and-restore.md
    name: taskfolk-safe-writes-and-restore
    method: generated
    api: openapi/taskfolk-product-api-openapi.yml
    description: >-
      What is reversible, for how long (30 days), and what is final. The reversibility skill —
      the thing an agent needs to know BEFORE it acts.
    operations:
      - 'DELETE /v1/workspaces/{slug}/projects/{key}/issues/{issueKey}'
      - 'POST /v1/workspaces/{slug}/projects/{key}/issues/{issueKey}/restore'
      - 'POST /v1/workspaces/{slug}/projects/{key}/restore'
      - 'POST /v1/workspaces/{slug}/docs/{id}/versions/{versionId}/restore'
      - 'POST /v1/workspaces/{slug}/webhooks/{id}/rotate-secret'
  - file: taskfolk-webhook-consumer.md
    name: taskfolk-webhook-consumer
    method: generated
    api: openapi/taskfolk-product-api-openapi.yml
    description: >-
      Register, verify (the HMAC key is SHA256(secret), not the secret), dedupe on the delivery
      UUID, and subscribe to the event names that actually fire.
    operations:
      - 'POST /v1/workspaces/{slug}/webhooks'
      - 'POST /v1/workspaces/{slug}/webhooks/{id}/test'
      - 'GET /v1/workspaces/{slug}/webhooks/{id}/deliveries'
      - 'POST /v1/workspaces/{slug}/webhooks/{id}/rotate-secret'
